"use client";

import ConnectButton from "./ConnectButton";

// We need a tiny wrapper to just render the connect button nicely inside our new drop down
export default function ConnectButtonWrapper() {
    return <ConnectButton />;
}
